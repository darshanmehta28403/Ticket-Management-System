import { Badge, Empty } from '../components/Shared.jsx';

export default function Dashboard({ total, tickets, onTicket }) { const resolved = tickets.filter(ticket => ticket.status === 'DONE').length; return <><section className="stats"><Stat label="All tickets" value={total} /><Stat label="Open tickets" value={total - resolved} /><Stat label="Resolved" value={resolved} /></section><section className="panel"><div className="section-heading"><div><h2>Recent tickets</h2><p>Track the latest work across your workspace.</p></div></div><div className="recent-list">{tickets.slice(0, 5).map(ticket => <button className="recent-row" key={ticket.id} onClick={() => onTicket(ticket)}><span><strong>{ticket.ticketNumber}</strong> {ticket.title}</span><Badge value={ticket.status} /></button>)}{!tickets.length && <Empty text="No tickets yet. Create the first ticket to start tracking work." />}</div></section></>; }
function Stat({ label, value }) { return <article className="stat"><p>{label}</p><strong>{value}</strong></article>; }
