import { PRIORITIES, STATUSES } from '../constants.js';
import { Actions, Modal, Select } from './Shared.jsx';

export default function TicketForm({ form, projects, users, ticket, setForm, submit, close }) { return <Modal title={ticket ? `Edit ${ticket.ticketNumber}` : 'New ticket'} close={close}><form className="form" onSubmit={submit}><label>Title<input required maxLength={140} value={form.title} onChange={e => setForm({ ...form, title: e.target.value })} /></label><label>Description<textarea required value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} /></label><div className="form-grid"><Select label="Priority" value={form.priority} values={PRIORITIES} onChange={value => setForm({ ...form, priority: value })} /><Select label="Status" value={form.status} values={STATUSES} onChange={value => setForm({ ...form, status: value })} /></div><label>Project<select required value={form.projectId} onChange={e => setForm({ ...form, projectId: e.target.value })}><option value="">Select a project</option>{projects.map(project => <option value={project.id} key={project.id}>{project.name}</option>)}</select></label><label>Assignee<select required value={form.assignedToId} onChange={e => setForm({ ...form, assignedToId: e.target.value })}><option value="">Select an assignee</option>{users.map(person => <option value={person.id} key={person.id}>{person.name}</option>)}</select></label><Actions close={close} submit={ticket ? 'Save changes' : 'Create ticket'} /></form></Modal>; }
